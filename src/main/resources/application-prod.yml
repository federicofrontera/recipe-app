spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ff_prod
    username: ff_prod_user
    password: fede
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false